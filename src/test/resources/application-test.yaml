spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: jdbc:h2:mem:jira-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: jira
    password: JiraRush
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true